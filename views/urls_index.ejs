<!DOCTYPE html>
<html lang="en">
<head>
  <title>Lilliputifier</title>
</head>
<body>
  <header>
    <%- include("partials/_header"); %>
  </header>

  <main>
    <h1>Currently Available Shortened URLs:</h1>

    <div class="new URLs">
      <h2>Lilliputagain!</h2>
      <p><a href="/urls/new">Click here to create a new shortened URL.</a></p>
    </div>

    <div class="master list">
      <h2>Lilliputesyoudid:</h2>
      <ul>
        <% for (record in urlDB) { %>
          <li><a href="/u/<%= urlDB[record].shortURL %>">www.lilli.put/<strong><%= urlDB[record].shortURL %></strong></a> 👉 <a href="<%= urlDB[record].longURL %>"><%= urlDB[record].longURL %></a> <br> <a href="/urls/<%= urlDB[record].shortURL %>">Lillipointsomewhereelse?</a> <form method="POST" action="/urls/<%= urlDB[record].shortURL %>/delete"><button type="submit">Delete</button></form><br></li>
        <% }; %>
      </ul>
    </div>
    </ul>
  </main>

  <footer>

  </footer>
</body>
</html>