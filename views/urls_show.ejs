<!DOCTYPE html>
<html lang="en">
<head>
  <title>Lilliputifier (Specific URL)</title>
</head>
<body>
  <header>
    <%- include("partials/_header"); %>
  </header>
  <h3>www.lilli.put/<strong><%= shortURL %></strong> points to <%= longURL %></h3>

  <form method="POST" action="/urls/<%= shortURL %>">
    <input type="text" value="<%= longURL %>" name="longURL">
      <button type="submit">Update</button></form>
</body>
</html>